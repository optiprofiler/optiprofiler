function y = alwaysCell(x)
    % `alwaysCell` function is defined for the unit test of `Problem.m`.
    % It activates codes that handles errors when using `double()`
    % function.

    y = {'a', 'b', 'c'};
end